# shorten-url

> Shorten a URL via tinyurl.fftp.io.
> The short URL is printed and copied to the clipboard.
> Requires an API key at ~/.config/tinyurl/api_key.

- Shorten a URL:

`shorten-url {{https://example.com/some/long/path}}`
