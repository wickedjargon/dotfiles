# bash completion for hotspot
_hotspot() {
    local cur
    COMPREPLY=()
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -eq 1 ]; then
        COMPREPLY=( $(compgen -W "on off password qr clients forget help --help" -- "$cur") )
        return
    fi
}

complete -F _hotspot hotspot
