# bash completion for color-picker
_color_picker() {
    local cur
    COMPREPLY=()
    cur="${COMP_WORDS[COMP_CWORD]}"

    # No arguments; only offer flags.
    if [[ "$cur" == -* ]]; then
        COMPREPLY=( $(compgen -W "--help" -- "$cur") )
    fi
}

complete -F _color_picker color-picker
