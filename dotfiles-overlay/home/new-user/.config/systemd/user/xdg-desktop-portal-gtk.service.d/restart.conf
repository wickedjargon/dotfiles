[Service]
# The upstream unit is static with no restart policy; if the backend dies
# (e.g. a client disconnects mid-request) every portal file dialog hangs
# until manually restarted.
Restart=on-failure
RestartSec=1
