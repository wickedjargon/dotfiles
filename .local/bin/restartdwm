#!/bin/sh

# iterate through each dwm instance and restart it
pgrep dwm | while read -r dwm_pid; do
    # kill the current dwm process
    kill -HUP "$dwm_pid"
done

# start a new instance of dwm
exec dwm &
