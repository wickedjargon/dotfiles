#!/bin/sh
. ~/.config/dmenu/config

selection="$( (printf "night\nday\n"; seq 5 5 100) | dmenu $DMENU_BASIC_ARGS -fn "$DMENU_FONT" -i -p 'Brightness: ' )"

case "$selection" in
    night) night ;;
    day) day ;;
    "") exit 1 ;;
    *) brightnessctl set "$selection"% ;;
esac
