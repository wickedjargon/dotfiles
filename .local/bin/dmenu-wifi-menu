#!/bin/sh
. ~/.config/dmenu/config

case "$(printf "dmenu-connect-to-network\nwifi-current-network\nwifi-disconnect\nwifi-rescan" | dmenu $DMENU_BASIC_ARGS -fn "$DMENU_FONT" -i -p 'Run: ')" in
    'dmenu-connect-to-network') dmenu-connect-to-network ;;
    'wifi-current-network') wifi-current-network ;;
    'wifi-disconnect') wifi-disconnect ;;
    'wifi-rescan') wifi-rescan ;;
    *) exit 1 ;;
esac
